{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/**", "/_next/**", "/images/**"]
  },
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["authenticated"]
    }
  ]
}
